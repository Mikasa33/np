import { createComponent } from '@vitepress/utils/component'
import TagDemo from './Tag.vue'
import TimeDemo from './Time.vue'

export const Tag = createComponent(TagDemo)
export const Time = createComponent(TimeDemo)
