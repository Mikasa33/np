import type { Component } from 'vue'
import { NpViewTag } from '../view-tag'
import { NpViewTime } from '../view-time'

export const components: Record<string, Component> = {
  NpViewTag,
  NpViewTime,
}
